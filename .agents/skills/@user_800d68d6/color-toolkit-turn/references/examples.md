# 输出示例

> 本文件展示 color-toolkit 各功能的输出格式示例。

## 颜色转换

输入 `#3498db`：

```json
{
  "input": "#3498db",
  "result": {
    "hex": "#3498db",
    "rgb": {"r": 52, "g": 152, "b": 219},
    "hsl": {"h": 204.0, "s": 69.8, "l": 53.1},
    "hsv": {"h": 204, "s": 76, "v": 86},
    "cmyk": {"c": 76, "m": 31, "y": 0, "k": 14},
    "luminance": "0.215",
    "grayscale": 130,
    "temperature": "冷色",
    "family": "蓝色系"
  }
}
```

## 对比度计算

计算 `#000000` 和 `#ffffff` 的对比度：

```json
{
  "color1": "#000000",
  "color2": "#ffffff",
  "algorithms": {
    "wcag2": {"value": "21.00:1", "level": "AAA级", "pass": true},
    "apca": {"value": 106.3, "level": "优秀", "pass": true},
    "cielab": {"value": "100.00 ΔE", "level": "极大差异", "pass": true},
    "ciede2000": {"value": "100.00 ΔE00", "level": "极大差异", "pass": true}
  }
}
```

## 颜色推荐

输入"科技感 蓝色"：

### JSON 数据结构

```json
{
  "request": "科技感 蓝色",
  "palette": {
    "primary": {"hex": "#0066FF", "name": "科技蓝"},
    "secondary": [
      {"hex": "#00D4FF", "name": "电光青"},
      {"hex": "#1A1A2E", "name": "深空灰"}
    ],
    "accent": {"hex": "#00FF88", "name": "信号绿"},
    "background": "#0A0A14",
    "text": "#FFFFFF"
  },
  "preview_url": "palette_preview.html"
}
```

### 可视化输出（HTML 预览页面）

实际输出是带色块和渐变效果的 HTML 页面，四色矩形布局如下（主色 × 2 辅助色 × 1 强调色 + 渐变 + 颜色代码）：

```
┌─────────────────────────────────────────────────┐
│        🎨 颜色色板 (4 色矩形网格)                   │
│                                                    │
│  ┌────────────┐  ┌────────────┐  ┌────────────┐  │
│  │  #0066FF   │  │  #00D4FF   │  │  #1A1A2E   │  │
│  │  (科技蓝)   │  │  (电光青)   │  │  (深空灰)   │  │
│  └────────────┘  └────────────┘  └────────────┘  │
│                                                    │
│  ┌────────────┐  ┌────────────┐                    │
│  │  #00FF88   │  │  #0A0A14   │                    │
│  │  (信号绿)   │  │  (背景色)   │                    │
│  └────────────┘  └────────────┘                    │
│                                                    │
│  🌈 渐变效果                                        │
│  ┌──────────────────────────────────────────┐      │
│  │ 135° 线性渐变                             │      │
│  └──────────────────────────────────────────┘      │
│  ┌──────────────────────────────────────────┐      │
│  │ 径向渐变                                  │      │
│  └──────────────────────────────────────────┘      │
│                                                    │
│  📋 颜色代码                                        │
│  1. #0066FF                                        │
│  2. #00D4FF                                        │
│  3. #1A1A2E                                        │
│  4. #00FF88                                        │
└─────────────────────────────────────────────────┘
```

每个色块是**矩形**（`<div class="color-preview">`，高 100px），内部显示 HEX 值，下方附带 RGB/HSL/CMYK/亮度详情。4 色自动网格布局。配色方案调用时，主色 + 2 辅助色 + 1 强调色 + 背景色 + 文字色一起展示。

## 调色板生成（色彩和谐方案）

基于色相环角度间隔，同一套饱和度/明度生成配色。

```python
get_palette("#3498db", "triadic")       # 三色组 120°
get_palette("#3498db", "tetradic")      # 矩形四色组 90°
get_palette("#3498db", "analogous")     # 类似色 30°
get_palette("#3498db", "complementary") # 互补色 180°
```

输入 `#3498db` 各类型的输出：

```
三色组 (Triadic, 120°):
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  #3498DB     │  │  34DB98      │  │  9834DB      │
│  蓝色        │  │  绿色        │  │  紫色        │
└──────────────┘  └──────────────┘  └──────────────┘

矩形四色组 (Tetradic, 90°):
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  #3498DB     │  │  34DBDB      │  │  DB6F34      │  │  DB346F      │
│  蓝色        │  │  青色        │  │  橙红        │  │  紫红        │
└──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘

互补色 (Complementary, 180°):
┌──────────────┐  ┌──────────────┐
│  #3498DB     │  │  DB6F34      │
│  蓝色        │  │  橙红        │
└──────────────┘  └──────────────┘

类似色 (Analogous, 30°):
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│  #3498DB     │  │  34C1DB      │  │  34DBB6      │
│  蓝色        │  │  蓝绿        │  │  青绿        │
└──────────────┘  └──────────────┘  └──────────────┘
```

## 颜色转换（预览页面）

输入 `#3498db` 时生成的 HTML 预览包含：

```
┌─────────────────────────────────────────────────┐
│           #3498DB  蓝色系 / 冷色                   │
│                                                    │
│  主色              互补色                            │
│  ┌──────────────┐  ┌──────────────┐                │
│  │  #3498DB     │  │  #DB6F34     │                │
│  └──────────────┘  └──────────────┘                │
│                                                    │
│  颜色编码值 (卡片布局)                                │
│  HEX  │  RGB          │  HSL         │  HSV       │
│  CMYK │  亮度 0.215   │  灰度 130    │  蓝色系     │
│                                                    │
│  🎭 配色方案: 三色组 / 类似色 / 渐变                 │
│  ♿ 无障碍对比: WCAG/APCA/CIELAB/CIEDE2000         │
│  🧩 UI组件预览: 按钮 / 卡片                          │
└─────────────────────────────────────────────────┘
```

## 对比度计算（预览页面）

两种颜色的对比度测试，HTML 输出包含：

```
┌─────────────────────────────────────────────────┐
│  对比度测试                                        │
│                                                    │
│  ┌──────────────┐  ┌──────────────┐               │
│  │ 背景: #000    │  │ 背景: #FFF   │               │
│  │ 前景: #FFF    │  │ 前景: #000   │               │
│  │  Aa 颜色预览  │  │  Aa 颜色预览  │               │
│  └──────────────┘  └──────────────┘               │
│                                                    │
│  对比度评估结果                                     │
│  ┌──────────┬──────────┬───────┬────────┐         │
│  │ 算法     │ 数值     │ 等级   │ 无障碍  │         │
│  ├──────────┼──────────┼───────┼────────┤         │
│  │ WCAG2.1  │ 21.00:1  │ AAA   │ ✅ 通过 │         │
│  │ APCA     │ 106.3Lc  │ 优秀  │ ✅ 通过 │         │
│  │ CIELAB   │ 100ΔE   │ 极大  │ -       │         │
│  │ CIEDE2000│ 100ΔE00 │ 极大  │ -       │         │
│  └──────────┴──────────┴───────┴────────┘         │
└─────────────────────────────────────────────────┘
```

## 互补色详情

输入 `#FF5733`：

```json
{
  "original": "#FF5733",
  "complementary": "#335CFF",
  "original_rgb": {"r": 255, "g": 87, "b": 51},
  "complementary_rgb": {"r": 51, "g": 92, "b": 255}
}
```

## 随机颜色

```json
["#E8574A", "#4AB89D", "#B84AC4", "#C4A84A", "#4A6BC4"]
```

各颜色色系/色温:
```
#E8574A | 红橙系 | 暖色 | 亮度 0.201
#4AB89D | 青绿系 | 冷色 | 亮度 0.421
#B84AC4 | 紫系   | 冷色 | 亮度 0.202
#C4A84A | 橙黄系 | 暖色 | 亮度 0.425
#4A6BC4 | 蓝色系 | 冷色 | 亮度 0.151
```

## 多颜色比较

输入 `#FF0000 #00FF00 #0000FF`：

```json
[
  {"color": "#FF0000", "hex": "#FF0000", "family": "红色系", "temperature": "暖色", "luminance": "0.213"},
  {"color": "#00FF00", "hex": "#00FF00", "family": "绿色系", "temperature": "中性", "luminance": "0.715"},
  {"color": "#0000FF", "hex": "#0000FF", "family": "蓝色系", "temperature": "冷色", "luminance": "0.072"}
]
```

生成预览: `compare_FF0000_00FF00.html`

## 格式验证

```python
ColorCore.is_valid_hex("#3498db")   # → True
ColorCore.is_valid_hex("#GGG")      # → False
ColorCore.is_valid_hex("notacolor") # → False
```

## 无障碍颜色推荐

固定白色背景，推荐 AA 级文字色（16px）：

```json
{
  "fixed_color": "#FFFFFF",
  "mode": "fg",
  "font_size": 16,
  "font_weight": "normal",
  "target": "AA",
  "is_large_text": false,
  "min_ratio": 4.5,
  "recommendations": [
    {"hex": "#943838", "name": "红", "contrast_ratio": "7.29:1", "level": "AAA"},
    {"hex": "#80714d", "name": "橙黄", "contrast_ratio": "4.72:1", "level": "AA"},
    {"hex": "#59804d", "name": "绿", "contrast_ratio": "4.55:1", "level": "AA"},
    {"hex": "#4d8066", "name": "青绿", "contrast_ratio": "4.57:1", "level": "AA"},
    {"hex": "#4d7380", "name": "蓝绿", "contrast_ratio": "5.15:1", "level": "AA"},
    {"hex": "#664d80", "name": "紫", "contrast_ratio": "5.09:1", "level": "AA"},
    {"hex": "#804d66", "name": "紫红", "contrast_ratio": "4.67:1", "level": "AA"}
  ],
  "total_candidates": 186,
  "total_found": 25
}
```
```
