# OPC 社区平台 · 产品需求文档（PRD）

| 项 | 内容 |
|---|---|
| 版本 | v0.4（修订稿） |
| 日期 | 2026-09-06 |
| 状态 | 待评审 |
| v0.2 修订 | **Agent 定位纠偏**：Agent 是 OPC 的自动化代理（执行臂），不是可独立承接与交付工作的主体。承诺、交付能力、信用、法律责任全部归属 OPC 本人。同步调整 §0、§2.2、§2.4（新增）、§3.2、§6.2、§6.4、§9 |
| v0.3 修订 | **运营后台与数据服务增补**：§4.1 新增年审 / 考核 / 分级处置 / 退出 / 清退机制（ID-10–14，V2 落地）；新增 §4.7 运营后台、企业画像与数据分析（OC-01–10）；§3.1 新增 Contract / OpsConsole / OpenPlatform 三个上下文；§1.4 非目标新增两条；§9 V2 范围同步。**V1 范围不变** |
| v0.4 修订 | **交付违约处置机制增补**（对"企业担心 OPC 无法按时交付 / 中途放弃"的对抗机制）：§4.3 新增 FU-12–15 与 §4.3.1 违约认定 / 违约金 / 善后移交机制（V1 P0）；合同模板内置违约金条款，**执行边界 = 未释放托管余额**；主动申报终止与失联违约差别对待；Contract 上下文新增 `BreachRecord`；§3.3 负面事件分档；§6.2 明确申报 / 申诉类动作不向 Agent 开放；§8 新增执行边界红线；SO-09 保证金维持 V2 的理由固化；澄清清单 D 组 D3 部分收敛。**V1 范围新增违约处置三项（FU-12–14）** |
| v0.4 增补（09-06） | **计费原语范围决策（D19）**：§1.4 / §3.1 / §3.3 / §4.3（FU-16）/ §5.3 / §9——B 端订阅**字段 V1 预留、功能 V2 落地**，效果付费仅预留合同字段；市场依据《OPC经营收费模式调研》（2026-09-05 核实版）。**V1 功能范围不变** |
| 方法论 | 双钻（Define 阶段收敛）／JTBD 双角色画像／机制设计（信用与撮合）／DDD 限界上下文／Agent-Native 范式 |
| 配套文档 | `00-需求澄清清单.md`、`资金与结算模式分析报告.md`、`OPC经营收费模式调研.md` |

---

## 0. 一页纸摘要

为 OPC（一人公司 / 超级个体）社区构建线上平台，打通**入驻认证 → 发单竞标 → 交付结算 → 信用沉淀 → 词元供给**的完整闭环。

与现有 OPC 社区（大连、北京、成都、杭州、重庆、威海等）全部"重线下、轻线上"不同，本平台把社区最宝贵的资产——**协作过程中产生的行为与履约数据**——沉淀为可计算、可对外验证的信用体系。

**三个差异化支点：**

1. **信用即基础设施** —— 信用体系同时服务站内治理与站外查询（API/MCP），业界目前缺的正是 Agent 的声誉层。
2. **Agent 原生** —— 不只面向人类用户。所有业务动作一次定义，同时暴露给 UI、REST、MCP、A2A、CLI，让超级个体把找单、投标、报名、对账这些重复操作自动化掉。**Agent 是 OPC 的执行臂，不是责任主体**：它代执行，不代承诺。
3. **AI 产能计入 OPC 的信用** —— 平台度量的是「OPC + 其 AI 工具链」的实际产出结果，而非工具本身。AI 放大的是 OPC 的产能，能力档案与信用归属于 OPC 这个人，不归属 Agent。

---

## 1. 项目背景与目标

### 1.1 背景

OPC（One Person Company，一人公司 / 超级个体）是依托 AI 大模型与云服务、由一人或极少数人运转的轻量化创业形态。2026 年以来，全国多地密集落地 OPC 社区：北京东城和平·in、成都双流芯谷、杭州临平才立方、重庆华新、威海智慧谷，以及大连高新区海创 OPC 社区。

**调研发现：现有 OPC 社区的共性做法是"五件套"——空间（免费工位）、算力（token 补贴）、订单（人工撮合）、培训（线下训练营）、金融/政策（基金与贴息），但几乎全部是线下交付 + 人工撮合，没有一体化线上平台，协作过程资产零沉淀。**

> **数据佐证（v0.3）**：复旦大学新闻学院姚建华团队《2026 年中国一人公司（OPC）调研报告》（新华社 2026-07 报道）：截至 2026-05 全国 OPC 社区 **426 个**，覆盖 28 省级行政区、65 城；**东北仅 8 个（占 1.9%）**，东部占 78.6%。报告指出「政策工具趋于同质化，部分 OPC 社区存在重建设、轻运营问题」，并建议政府「从'给工位、重补贴'走向'给订单、建生态'」——与本项目价值主张一致。注：「五件套」为我方网络调研归纳，与复旦报告口径（注册与空间 / 专业服务与能力培育 / AI 要素供给 / 应用场景 / 金融与风险）相近但不完全一致。

大连本地锚点：《大连高新区支持 OPC 发展若干措施（试行）》（大高管规发〔2026〕1 号，2026-05-16 发布，**试行期 2026-06-15 至 2028-06-14**），十条措施（另附则一条）包括：

| 条款 | 内容 | 对平台的意义 |
|---|---|---|
| 第一条 | 免费工位 ≥3 个月 | 入驻模块需管理工位资源 |
| 第二条 | 按算力采购额 50% 补贴"token 加油包"，每家每年 ≤20 万，**季度兑现** | **Token 消耗台账 = 补贴申报凭证**，是极强的入驻锁定 |
| 第三条 | 年度遴选，入选 OPC 奖励 10 万 | 与信用/认证体系天然挂钩 |
| 第四条 | 政府公共数据资源向 OPC 社区开放；支持参与大厂大企场景建设，按投入 35% 补贴，每家每年 ≤50 万 | **冷启动的需求来源** |
| 第五条 | ≤1000 万投资；贷款按 1% 年化贴息，年度 ≤30 万 | 金融服务入口 |
| 第六条 | "超级训练营"免费 AI 与创业培训 | 公开课模块的政策依据 |

### 1.2 问题陈述

| # | 痛点 | 证据 |
|---|---|---|
| P1 | **有技术、找不到订单** —— 单打独斗陷入信息孤岛 | 威海智慧谷、大连高新区案例均提到此困境 |
| P2 | **不敢接、接不起** —— 企业不敢把活交给一个人；OPC 扛不住 30–90 天账期 | 一人公司现金流脆弱，是结构性劣势 |
| P3 | **能力无法被证明** —— 没有可携带的履约记录，每单都从零建立信任 | 信用无法跨项目、跨平台积累 |
| P4 | **资源用不起来** —— 算力补贴、培训课程、社区资源分散，无统一入口 | 政策兑现靠线下申报 |

### 1.3 产品目标

| 层级 | 目标 |
|---|---|
| 对 OPC | 拿到订单、证明能力、用得起算力、扛得住账期 |
| 对发包方 | 敢把活交给一人公司——因为平台提供资质核验、过程留痕、资金担保、争议裁决、违约处置（FU-12–14） |
| 对社区/园区 | 政策线上化兑现、生态数据可度量、形成可复制的区域样板 |
| 对外部系统与 Agent | 提供可查询的 OPC 能力与信用数据（差异化战略支點） |

### 1.4 非目标（V1 明确不做）

- 不做社交/IM 社区（专注交易协作，不抢微信群生态位）
- 不做平台总包与垫付（V3 方向，见 §9）
- **不做订阅类订单交易**（V2 方向，FU-16）——V1 撮合仅支持项目制计费（里程碑分期 + 分档结算）；订单 / 合同模型预留「计费方式 / 计费周期 / 续费条款」结构化字段，订阅的周期扣费、自动续费、订阅争议规则 V2 落地（D19）
- **不做项目管理 SaaS**（J4 已定）——运营后台只做运营视角的风控抓手与数据出口，不替代 OPC 自己的 Teambition / 飞书项目
- 不采购宏观行业数据库、不自建行业研究——行业趋势分析仅基于平台内交易与行为数据的匿名汇总（D17，见 §4.7）
- **不做 Agent 自主议价、自主签约、自主交付结算** —— Agent 只代执行，承诺必须由人做出（见 §6.4）
- 不治理 OPC 的生产侧 AI 工具链，只度量其产出结果（见 §2.4）
- 不自建支付/VC 体系（全部接入持牌基础设施）

### 1.5 成功指标

| 类别 | 指标 | 目标（首年） |
|---|---|---|
| 供给 | 认证 OPC 数 | ≥300 |
| 需求 | 撮合 GMV | ≥2000 万 |
| 撮合 | 订单成交率（发标→成交） | ≥60% |
| 交付 | 里程碑一次验收通过率 | ≥75% |
| 交付 | 中途终止订单处置闭环时效（认定→资金处置→移交，§4.3.1） | T+7 内完成，达成率 ≥90% |
| 信用 | 有 ≥1 条履约记录的 OPC 占比 | ≥50% |
| Token | 词元交易 GMV / 活跃采购 OPC 数 | 见商务目标 |
| Agent | MCP 月活调用方 / 通过 Agent 提交的投标占比 | ≥15% 投标来自 Agent |

---

## 2. 用户与角色

### 2.1 人类角色

| 角色 | 核心 Job To Be Done | 关键诉求 |
|---|---|---|
| **OPC 主体** | "让我持续拿到能做的单，并且我的能力被看见" | 快速匹配合适订单、履约有记录、结算有保障 |
| **发包方**（园区企业/政府/大厂） | "把活可靠地交出去，别让我承担选错人的风险" | 资质可信、过程可控、资金安全、争议有裁决 |
| **社区运营** | "让生态转起来，政策能兑现、数据说得清" | 审批效率、活动组织、生态看板 |
| **审批人**（社区/园区） | "快速判断这个申请人该不该放进社区" | 材料齐全、流程清晰、责任留痕 |
| **内容提供者**（讲师/OPC） | "我的知识输出能带来回报与声誉" | 课程工具、分成、贡献被记录 |
| **Token 供应商** | "多一个稳定的分发渠道，别增加我的对接成本" | 接入简单、结算及时、数据透明 |

> **术语说明（v0.3）**：本平台不设独立的「会员」层级——入驻主体（Principal）即社区成员，入驻申请 / 评审 / 入驻流程见 §4.1。是否在入驻之上叠加付费会员分级，取决于商业模式决策（澄清清单 A4 / B4，待定）；若后续引入，将作为权益层叠加在认证等级之上，不改变主体模型。

### 2.2 Agent 角色（本平台的差异化所在）

> **定位：Agent 是 OPC 的自动化代理，不是平台上的第二类主体。**
> 它可以替 OPC 执行动作，但**不能替 OPC 做出承诺**——投标、签约、交付、结算的法律后果，100% 由 OPC 本人承担。

| 角色 | 说明 | V1 权限 |
|---|---|---|
| **OPC 的代理 Agent** | OPC 授权的自动化代理，替他执行平台上的重复性操作 | **L2 代执行**：查询发包、提交投标、报名课程、领取任务、上传交付物、查询自身订单 |
| **发包方的代理 Agent** | 发包方授权的代理，代为发布需求、查看投标、进度跟踪 | **L2 代执行** |
| **外部查询 Agent** | 第三方系统/Agent，查询 OPC 的公开能力与信用档案 | L1 只读（公开信用档案、认证等级） |
| **供应商 Agent** | Token 供应商提供的计量/对账 Agent | 回调用量、对账 |

**明确不做**：Agent 不拥有独立的能力档案、不承接订单、不签署合同、不发起结算、不参与争议主体认定。

### 2.3 双角色 JTBD（设计约束）

> **同一个业务动作，必须同时回答两个问题：**
> - 人类用户"雇佣"这个平台来完成什么 Job？
> - 该动作交由 Agent 代执行时，需要什么形状的接口、什么样的错误反馈、什么样的确定性保证？

这直接导出 §6 的 Agent-Native 设计要求：**工具面要精简、描述为 LLM 推理而写、写操作必须幂等、错误必须结构化、权限必须作用域化。**

### 2.4 关键边界：生产侧 AI vs 接入侧 Agent

OPC 场景下这两件事极易混淆，必须在设计上严格分开：

| | 生产侧 AI | 接入侧 Agent |
|---|---|---|
| 是什么 | OPC 用来**产出交付物**的 AI 工具链（如 AI 漫剧生成、AI 编标、AI 数据分析） | 调用本平台接口的**自动化代理** |
| 归谁所有 | OPC 自己，平台无从也无需知晓 | 绑定到 OPC 的凭证，平台登记 |
| 平台是否治理 | **不治理**。这是 OPC 的生产工具与商业秘密 | **必须治理**：注册、授权、限流、审计 |
| 平台关心什么 | 只关心**产出结果**（交付物是否通过验收） | 关心**行为合法性**（是否在授权范围内、是否可追溯） |
| 举例 | 做 AI 漫剧的 OPC 用自研流程出片 | 同一个 OPC 的 Agent 自动帮他投标数据标注任务 |

> **这条边界的意义**：大连 OPC 社区的三大方向（AI+文娱 / AI+数据 / AI+跨境）中，大量交付物本身就是 AI 生成的。但那是 **OPC 的产能**，不是平台的 Agent 在交付。平台越界去治理生产侧 AI，既不可行也会引发 OPC 的强烈抵触。
>
> 因此：**能力档案与信用记录的是「这个 OPC 交付了什么、质量如何」，而不是「他用了什么工具」。**

---

## 3. 核心概念与领域模型

### 3.1 限界上下文（DDD）

| 上下文 | 职责 | 关键聚合根 |
|---|---|---|
| **身份与入驻 Identity** | 主体注册、实名、资质材料、审批流 | `Principal`、`Application` |
| **商机与竞标 Sourcing** | 需求发布、投标、评标、授标 | `Order`、`Bid`、`Award` |
| **交付与履约 Fulfillment** | 里程碑、交付物、验收、争议 | `Milestone`、`Deliverable`、`Dispute` |
| **信用与认证 Credit** | 积分账本、信用分、认证等级、权益。**覆盖两套评价模型**：乙方 OPC（§3.3–3.5）与甲方发包方（§3.6） | `CreditLedger`、`CreditProfile`、`Tier`、`ClientCreditProfile` |
| **结算 Settlement** | 资金指令、分账、账单、发票 | `Escrow`、`Settlement`、`Invoice` |
| **词元市场 TokenMarket** | 供应商、商品、计量、计费 | `Supplier`、`TokenProduct`、`Usage` |
| **内容与社区 Content** | 课程、活动、新闻 | `Course`、`Event`、`Article` |
| **Agent 接入 AgentGateway** | Agent 注册、授权、Action 编排、审计 | `Agent`、`Grant`、`ActionLog` |
| **合同与履约事实 Contract**（v0.3 新增） | 合同结构化字段、签署状态、履约事实存证，支撑四流一致与争议裁决。**810 号令第七条要求税务检查时平台须提供「合同订单」——合同结构化是法定要求，非增值功能**。结构化字段自 V1 起含**计费方式 / 计费周期 / 续费条款**与**结果定义 / 计量源 / 归因例外**预留位（v0.4：前者支撑 V2 订阅扩展，FU-16；后者为效果付费 V2 后议的合同预留，**功能不排期**——归因难 + 指标标准化难。依据：Gartner 2027 clawback 预测 + 810 号令第七条，来源《OPC经营收费模式调研》§3.5） | `Contract`、`FactRecord`、`BreachRecord`（v0.4：违约认定与处置记录，复用 FactRecord 存证规范） |
| **平台运营后台 OpsConsole**（v0.3 新增） | 运营介入工具、生态看板、双侧企业画像、匿名行业趋势、报送导出（§4.7） | `Case`、`Dashboard`、`ProfileView` |
| **开放平台 OpenPlatform**（V1 接口预留） | API / Webhook / SSO 预留；典型集成（飞书 / 钉钉 / 金蝶）放 V2（澄清清单 K 组） | `App`、`Webhook`、`OrgConnection` |

**设计原则：Credit 是横切内核，其他上下文通过领域事件向其投递贡献记录；Credit 不反向依赖任何业务上下文。**

### 3.2 单一主体 + 代理通道模型（核心抽象）

> **纠偏说明**：v0.1 曾将模型表述为「人 + Agent 的复合主体」，该表述已废弃。
> **Principal（OPC 本人/公司）是平台上唯一的主体，也是唯一的责任主体。Agent 是挂在 Principal 名下的执行通道，不是主体。**

```
Principal（主体：个人 / 个体工商户 / 一人公司 / 小微团队）  ← 唯一责任主体
   ├── 实名等级 RealNameLevel: R1 手机+人脸 | R2 工商信息核验 | R3 银行四要素或对公打款验证
   ├── 资质材料 Qualification[]
   ├── 能力档案 CapabilityProfile    ← 记录"这个 OPC 能交付什么"，含 AI 加持后的产能
   ├── 信用档案 CreditProfile        ← 信用归属于 Principal，不归属 Agent
   │
   └── AgentGrant[]  （执行通道，非主体）
          ├── agent_id
          ├── level: L1 代查询 | L2 代执行 | L3 规则内自动化
          ├── scope: 被授权的 Action 列表
          ├── rules: 仅 L3 —— 人类预设的触发条件与约束（见 §6.4）
          └── 凭证: API Key / OAuth Grant（可随时撤销）
```

**所有写操作的行为日志统一记录三元组：**

```json
{ "principal_id": "...", "agent_id": "..." | null, "channel": "ui" | "api" | "mcp" | "a2a" | "cli" | "webhook" }
```

> `agent_id` **必须全量落库**，目的不是给 Agent 建信用，而是：
> 1. **争议追溯** —— 出问题时能回答"这个动作是人点的，还是 Agent 在授权范围内自动执行的"
> 2. **授权审计** —— 能回答"该 Agent 当时的 scope 是什么，是否越权"
> 3. **未来可回溯** —— 若将来业务需要按 Agent 维度分析行为，历史数据仍然可用

**责任归属铁律**：Agent 的任何动作，其法律后果由 Principal 承担。平台不因"是 Agent 操作的"而减轻或转移 Principal 的责任，Agent 也不因执行动作而获得任何独立权利。

### 3.3 信用体系的三层结构

| 层 | 本质 | 是否可变 | 说明 |
|---|---|---|---|
| **积分 Points** | 原始行为记录（append-only 账本） | 不可改，只能追加冲正条目 | **纯荣誉型：不可消耗、不可转让、不可兑换、不可提现** |
| **信用分 CreditScore** | 由积分加权 + 时间衰减计算得出的视图 | 随计算变化 | 0–1000 分，算法公开可解释 |
| **认证等级 Tier** | 信用分 + 实名等级 + 资质门槛的分档 | 定期重算 + 事件触发 | **T0** 待认证 / **T1** 铜 / **T2** 银 / **T3** 金 / **T4** 白金 |

> **命名规范（全文档统一，避免三套量表混淆）**
> | 前缀 | 量表 | 取值 |
> |---|---|---|
> | **R** | 实名等级 RealNameLevel | R1 / R2 / R3 |
> | **T** | 认证等级 Tier | T0 / T1 / T2 / T3 / T4 |
> | **L** | Agent 授权级别 | L1 代查询 / L2 代执行 / L3 规则内自动化 |

**衰减函数**（防"一劳永逸"）：

```
score = Σ (event_value_i × weight_i × e^(-λ × age_days_i))
λ 默认按 180 天半衰期取值 ≈ 0.00385，后台可配
```

**积分获取项与初始权重**（权重配置化，不改代码即可调整；冷启动期重学习成长，成熟期向交付履约倾斜）：

| 类别 | 事件示例 | 权重（初期 → 成熟期） |
|---|---|---|
| 交付履约 | 里程碑验收通过（按金额分档）、验收满意度、按时交付率 | 30% → 55% |
| 学习成长 | 完成课程、通过考核、获得证书 | 30% → 15% |
| 社区贡献 | 提供培训、答疑被采纳、活动组织、内容输出 | 25% → 15% |
| 生态贡献 | 作为 Token 供应商接入、发布场景订单、推荐入驻成功 | 15% → 15% |
| 负面（扣减） | 违约（v0.4 分档：主动申报终止 / 失联违约，措辞与权重不同，见 §4.3.1）、逾期、差评、投诉成立、刷分 | 独立扣分项，不参与归一化 |

> **设计要点**：积分是**原始账本**而非余额。任何"扣分"都以追加负向条目实现，永不改写历史——这既是审计要求，也是未来导出为 VC 的前提。

> **预留维度（v0.4）**：**续费率 / 订阅存续时长**——订阅制下 OPC 的持续交付质量是高价值信用输入（订阅制为市场调研中 OPC 提及最多、占比最高的收费模式，见论据核实记录 #10）；随订阅计费能力 FU-16（V2）一并启用，启用前不计分。

> **注**：§3.3–3.5 描述的是**乙方（OPC）**的信用模型。**甲方（发包方）同样采用"积分账本 / 信用分 / 等级"的三层结构**，但指标体系、权重与数据来源不同，详见 §3.6。两套模型共用同一套计算引擎与账本基础设施。

### 3.4 积分 → 权益映射（纯荣誉型的关键补偿设计）

因为积分不可兑换，**驱动力必须来自"权限与身份"而非"兑换"**。下表是 V1 必须实现的核心功能，否则信用体系会空转：

| 权益 | T1 铜 | T2 银 | T3 金 | T4 白金 |
|---|---|---|---|---|
| 同时承接订单数上限 | 2 | 5 | 10 | 不限 |
| 单笔订单金额上限 | 1 万 | 10 万 | 50 万 | 不限 |
| 竞标列表排序加权 | — | +5% | +12% | +20% |
| 平台佣金费率 | 基准 | −10% | −20% | −30% |
| 定向邀标被推荐概率 | 低 | 中 | 高 | 最高 |
| 专属标识 / 优先展示 | — | ✓ | ✓ | ✓ |
| Agent 规则内自动化（L3）申请资格 | — | — | ✓ | ✓ |
| 政策推荐名额（10 万奖励等） | — | — | ✓ | ✓ |
| 免费工位续期优先权 | — | ✓ | ✓ | ✓ |

### 3.5 女巫抵抗

信用体系的价值取决于防刷能力。分层设计：

| 实名等级 | 验证方式 | 解锁能力 |
|---|---|---|
| **R1** | 手机号 + 人脸核身 | 浏览、报名课程、T1 认证 |
| **R2** | + 工商信息 / 个体户核验 | 投标、接单、T2 认证 |
| **R3** | + 银行四要素或对公打款验证 | 大额订单、提现结算、T3+ 认证 |

**配套反作弊规则（V1 必须实现）**：同设备/同 IP/同收款账户关联检测；互评环检测（A↔B 互相好评）；异常投标模式检测（高频低质投标）；贡献图谱分析（借鉴 SourceCred 的贡献流思想，识别无下游价值的自循环贡献）。

### 3.6 甲方（发包方）信用体系

> **为什么必须有**：只给乙方建信用是**不对称的缺陷**，会直接导致柠檬市场。
>
> OPC 是一人公司，抗风险能力极弱，一次拖款就可能压垮他。如果平台上只有乙方被评分，劣质甲方（拖款、反复改需求、无理拒收）会大量涌入，**优质 OPC 被劣币驱逐**，平台最终只剩低端供给。众包平台研究的结论也是一致的：信用评价指标必须**从发包方与接包方两方面出发**。

#### 3.6.1 维度与权重

与乙方信用不同，**甲方信用的权重集中在"付款"与"验收公允"**——这是 OPC 最关心的两件事。

| 维度 | 权重 | 具体指标 | 数据来源 | 客观性 |
|---|---|---|---|---|
| **付款履约** | 35% | 按时付款率、平均逾期天数、里程碑确认时长 | 结算链路（若托管）／乙方确认（若不托管） | **高 → 低**（见 §3.6.4） |
| **验收公允性** | 25% | 一次验收通过率、争议发生率、**争议败诉率**、验收后追加修改次数 | 平台验收流程 + 争议裁决结果 | **高** |
| **需求质量** | 20% | 需求文档完整度、签约后变更次数与幅度、流标率、撤单率 | 订单结构化字段 + 变更记录 + 乙方评价 | 中 |
| **主体资质与合规** | 10% | 工商核验、司法失信记录、经营异常、实控人关联 | **外部数据对接** | **高** |
| **配合响应** | 10% | 沟通响应时长、资料/权限提供准时率、过程反馈及时性 | 沟通记录时间戳 + 乙方评价 | 中 |

> **"争议败诉率"是最客观的单一指标** —— 它不是任何一方的主观评价，而是平台裁决的结果。建议单列展示。

#### 3.6.2 冷启动：甲方没有历史数据怎么办

| 阶段 | 做法 |
|---|---|
| 首次发单前 | 工商核验 + 司法失信 + 经营异常 + 实控人关联检测 → 得出**初始信用**（主要来自外部数据，可立即给出） |
| 前 N 单（默认 3 单） | **强制里程碑托管 + 预付款**，作为保证金的替代——用资金约束弥补信用数据缺失 |
| 无履约记录时 | 订单对 OPC 展示时明确标注"暂无履约记录"，避免 OPC 误判 |

**建议对接的外部数据源**：国家企业信用信息公示系统（工商信息、经营异常）、中国执行信息公开网（失信被执行人）、裁判文书网（合同纠纷）。这些是低成本高价值的数据，且**在甲方注册时即可获取**，不需要等交易发生。

#### 3.6.3 博弈风险与缓解

| 风险 | 说明 | 缓解机制 |
|---|---|---|
| **评价报复** | 甲方给差评 → 乙方报复性差评 | **双向盲评**：双方都提交评价后才互相可见 |
| **乙方不敢给差评** | OPC 弱势，怕影响后续接单 | 盲评 + 延迟公开 + 仅完成交易的双方可评 + 评价聚合展示（不暴露单条归属） |
| **甲方刷好评** | 用关联主体互相发单刷分 | 实控人关联检测 + **只统计有真实资金流且已完结的订单** |
| **甲方威胁退出** | 信用分低被限制后流失 | 信用分**先只影响排序与展示，不硬性限制发单**；提供申诉与改进路径，给改进留出口 |
| **规避平台私下结算** | 甲方与 OPC 熟了之后线下交易 | 异常模式检测（长期沟通活跃但平台无新增订单）+ 正向激励（平台托管带来的合规发票与补贴申报） |

#### 3.6.4 关键约束：付款维度依赖资金托管

> **这一条会影响 §5 结算路径的选型，需要业务方重新权衡。**

付款履约是甲方信用里最硬、权重最高（35%）的维度，但它的数据**只能来自平台的结算链路**：

| 结算方案 | 付款维度的数据质量 | 后果 |
|---|---|---|
| `LicensedSplit` / `BankCustody`（资金托管） | 平台掌握**逐笔的付款时点、逾期天数、确认时长**，客观、实时、不可篡改 | 甲方信用体系完整 |
| `None`（纯撮合不碰钱） | 只能靠乙方**事后手动确认**，数据滞后、易漏报，且乙方处于弱势**易被胁迫虚报** | 甲方信用价值大幅下降 |

**这实际上强化了"应该上分账托管"的论证**：资金托管不只是对乙方的保护，它同时是**甲方信用体系的数据基础设施**。没有它，甲方信用退化为一套主观互评，防不住真正的劣质甲方。

**若 V1 确实选择不碰钱，降级方案**：付款维度权重下调至 15%，把释放的权重移到"验收公允性"与"需求质量"（这两个维度不依赖资金流），并在订单页对 OPC 明确提示"该甲方付款数据未经平台验证"。

#### 3.6.5 甲方信用的用途（这是吸引供给的强差异化）

**对 OPC（最重要）**：投标前就能看到甲方的履约画像——

```
该甲方 · 履约画像
  付款速度        平均 2 天内确认，无逾期记录
  一次验收通过率   78%（高于平台均值 65%）
  需求变更        平均 1.2 次/单
  争议率          3%，近 12 个月无败诉
  已完结订单      27 单 · 累计 ¥340 万
```

> **这是平台能给 OPC 的最实用的信息优势。** 一人公司最怕的不是活难，是**接错单**——干完了拿不到钱、需求无限蔓延、验收被反复刁难。投标前的这个画像，直接把"接错单"的概率降下来。这也反向约束甲方改善自己的行为。

**对平台**：风控（低分甲方强制预付款托管/保证金）、排序（高信用甲方的订单优先推荐给优质 OPC）、定价依据。

**对甲方**：高信用甲方能吸引更多优质 OPC 投标，解决"招不到靠谱的人"的问题——这是让甲方主动改善行为的正向激励，而非单纯约束。

---

## 4. 功能需求

### 4.1 入驻与资质

| 编号 | 需求 | 优先级 |
|---|---|---|
| ID-01 | 在线提交入驻申请：主体类型、基本info、方向标签（AI+文娱/AI+数据/AI+跨境等）、材料上传 | P0 |
| ID-02 | 多级审批流：可配置审批节点与角色（社区运营 → 园区管委会 → 专家评审） | P0 |
| ID-03 | 申请状态全链路可追踪，状态变更主动通知（站内 + 短信 + Webhook） | P0 |
| ID-04 | 材料在线核验：对接工商/个体户信息核验接口，减少人工审核 | P1 |
| ID-05 | 电子签约：入驻协议在线签署（对接第三方 CA / 电子签服务） | P1 |
| ID-06 | 入驻档案与信用档案自动关联，入驻即建立 `CreditProfile` | P0 |
| ID-07 | 年审与清退（总纲，v0.3 细化为 ID-10–13） | P2 |
| ID-08 | 工位/场地资源管理（对接大连政策第一条） | P2 |
| ID-09 | **Agent 侧**：申请状态查询 Action（只读） | P0 |
| ID-10 | **年审与考核**：年审周期、考核维度（信用分 + 活跃度 + 合规负面）与阈值后台可配；考核结果联动认证等级升降级（CR-05）与权益映射 | P2 |
| ID-11 | **分级处置**：考核不通过按梯度处置——限权 → 限单 → 限期整改 → 清退，全程留痕并通知本人，给申诉与改进路径 | P2 |
| ID-12 | **主动退出**：结清在途订单与托管资金 → 信用档案封存（历史可查询、标注「已退出」）→ 数据按留存政策处理 | P2 |
| ID-13 | **条件清退**：触发红线（严重违约、刷分、违法）时平台单方清退，公示并设申诉期；重新入驻设冷却期 | P2 |
| ID-14 | **Agent 侧**：年审通知、考核结果查询 Action（只读） | P2 |

**关键流程**：提交 → 材料完整性自动校验 → 人工审批（可多级）→ 签约 → 建立主体档案 + 信用档案 → 获得初始认证等级 L1

> **设计要点（v0.3）**：年审周期、考核指标与清退条件需业务方拍板（澄清清单 B3）。在专门考核机制上线前，**信用体系的自动升降级（CR-03 / CR-05，T0–T4）即事实上的持续考核**——信用分定期重算 + 事件触发，等级与权益实时联动，不需要另建一套并行考核。ID-10–14 全部为 V2 落地，V1 不做，但订单 / 信用 / 合规数据从 V1 起全量留痕，保证未来可回溯考核。

### 4.2 发单与竞标

| 编号 | 需求 | 优先级 |
|---|---|---|
| SO-01 | 需求发布：标题、描述、行业、技能标签、预算区间、工期、里程碑模板、交付标准 | P0 |
| SO-02 | 平台合规初审（预算真实性、需求合规性）后上架，状态：草稿/待审/竞标中/已授标/进行中/已完成/已流标 | P0 |
| SO-03 | **公开竞标（主路径）**：OPC 提交投标方案（含报价、工期、方案说明、相关案例） | P0 |
| SO-04 | **定向邀标**：发包方可指定 OPC 名单发标，被邀标方收到定向通知 | P0 |
| SO-05 | 投标期管理：截止时间、延期、撤标、流标处理 | P0 |
| SO-06 | 评标辅助：按信用分/历史履约/报价/方案匹配度生成候选排序建议（**辅助决策，非自动定标**） | P1 |
| SO-07 | 在线洽谈：投标期间的问答与澄清记录（全程留痕，作为争议裁决依据） | P1 |
| SO-08 | 授标：发包方选定 → 生成合同（平台模板或双方上传）→ 进入交付 | P0 |
| SO-09 | 投标保证金（可选，按订单金额与 OPC 信用等级动态判定是否收取）。**v0.4：维持 V2**——V1 的交付违约对抗杠杆为 FU-13 违约金（托管扣款）+ FU-14 投标资格冻结，保证金对冷启动期供给端是纯摩擦 | P2 |
| SO-10 | **Agent 侧**：`search_orders`、`get_order_detail`、`list_my_bids`、`submit_bid`（幂等） | P0 |
| SO-11 | **甲方履约画像展示**：订单详情页向 OPC 展示甲方的付款速度、一次验收通过率、需求变更频率、争议率与败诉率、历史完结订单量（样式见 §3.6.5） | P0 |
| SO-12 | 需求质量结构化：发单时强制填写交付标准与验收标准（否则无法上架），作为"需求完整度"评分的数据基础 | P0 |

**关键流程**：发包 → 平台初审 → 竞标中（公开 / 定向）→ 投标 → 评标（人工为主，排序建议辅助）→ 授标 → 签约

### 4.3 交付与结算

| 编号 | 需求 | 优先级 |
|---|---|---|
| FU-01 | 里程碑管理：支持平台行业模板库 + 自定义，含交付标准与验收标准 | P0 |
| FU-02 | 交付物提交与版本管理，支持多次提交与打回整改 | P0 |
| FU-03 | 阶段验收：发包方在线核验，通过则标记阶段完结，不合格填写整改意见退回 | P0 |
| FU-04 | **分档结算**：小额自动结算 / 大额里程碑托管 + 人工抽检，阈值后台可配（默认 1 万元） | P0 |
| FU-05 | **结算适配器（可插拔）**：抽象 `createEscrow / releaseMilestone / refund / split / getStatus` 接口，支持四种实现（见 §5.3） | P0 |
| FU-06 | 争议发起与裁决：提交证据 → 平台/专家委员会裁决 → 执行资金处置 | P0 |
| FU-07 | **双向评价**（双向盲评，双方都提交后才互相可见）：乙方评甲方的付款及时性、需求清晰度、验收公允性；甲方评乙方的质量、响应速度、沟通。评价分别进入**两套**信用计算（§3.3 乙方 / §3.6 甲方） | P0 |
| FU-11 | 需求变更记录：签约后的 scope 变更、工期调整、金额调整全部线上留痕，作为甲方"需求质量"维度的数据来源 | P0 |
| FU-08 | 发票管理：记录发票流向（谁开给谁、金额、税号、开票时间），支持附件归档 | P0 |
| FU-09 | 资金台账与对账单导出 | P1 |
| FU-10 | **Agent 侧**：`submit_deliverable`、`get_milestone_status`（L2）；`request_settlement` 仅预留不开放 | P1 |
| FU-12 | **违约认定与催告**（v0.4）：四类违约的客观触发器（逾期超宽限 / 主动申报终止 / 失联推定 / 里程碑多次打回，见 §4.3.1）；三通道催告（站内 + 短信 + Webhook）；《违约认定书》自动生成（触发器、证据指针、金额计算式）；大额（≥ 结算阈值）运营双人复核（OC-01），小额自动认定 | P0 |
| FU-13 | **违约金与资金处置**（v0.4）：合同模板内置违约金条款（默认费率与封顶见 §4.3.1，后台可配）；**执行边界 = 未释放托管余额**，超出部分由发包方经 FU-06 按实际损失举证主张；先裁后扣（大额）/ 先扣后诉（小额，申诉期默认 7 天）；申诉期资金在持牌机构监管户暂存（依赖 LicensedSplit 延迟分账，§5.3）；扣款指令写入 `BreachRecord`；发票冲红 / 作废归档（四流一致） | P0 |
| FU-14 | **中途放弃善后与移交**（v0.4）：证据封存（合同、里程碑记录、交付物版本史、洽谈记录、Agent 三元组日志，append-only）；资金处置（已验收未释放款正常释放、违约金扣除、余额默认退发包方、发包方可选择支付部分换取可用半成品）；结构化移交清单（按订单类型模板：源码 / 设计源文件 / 账号凭证 / 数据 / 部署配置 / 文档）；**移交完成前冻结该 OPC 新投标资格**；信用负面条目（分档）；发包方一键复制需求重发新单并标注历史终止 | P0 |
| FU-15 | **Agent 侧**（v0.4）：违约认定 / 处置状态查询 Action（只读）、催告与认定通知 Webhook。**主动申报终止与违约申诉必须由 Principal 本人在 UI 做出**，不向 Agent 开放（§6.2） | P1 |
| FU-16 | **周期订阅计费原语**（v0.4，V2 落地）：订阅类订单完整能力——周期扣费、自动续费、按周期释放、续费率统计、订阅争议规则（无离散验收点下的服务中断 / 质量争议处置）。**V1 仅做字段预留**：订单 / 合同模型内置「计费方式 / 计费周期 / 续费条款 / 自动续费标志」结构化字段（与效果付费「结果定义 + 计量源」预留同策略）；结算适配器届时追加代扣 + 分账组合原语（§5.3）；自动续费合规口径待法务确认（X14） | P2 |

**关键流程**：里程碑定义 → 交付 → 验收（通过/打回）→ 触发结算指令 → 资金释放 → 评价 → 信用记账；**违约分支**（FU-12–14）：触发器命中 → 催告 → 认定（申诉期）→ 资金处置 → 成果移交 → 信用负面记账 → 发包方重新撮合（§4.3.1）

> **计费原语范围（v0.4 决策，D19）**：V1 撮合只支持**项目制**计费（里程碑分期 + 分档结算，FU-01–05）——与冷启动订单结构一致（大连政府采购四类场景均为项目制交付；大连本地 OPC 实际也以 B 端项目制为主，几乎无订阅实践）。订阅制是市场调研中 OPC 提及最多（口径 A 75 次）、也是最主要（口径 B 44%）的收费模式（WAIC《2026 中国 OPC 深度洞察报告》，论据核实记录 #10；对外引用须两口径并述），平台必须覆盖、但**不进 V1**：字段预留、V2 落地（FU-16），续费率信用维度随同启用（§3.3）。与「平台自身向 B 端售卖订阅 / 会员」是两个命题，后者仍属商业外延决策（澄清清单 A4 / B4）。

> **后继序列（v0.4 增补）**：订阅（FU-16，V2）→ 数字商品一次性购买（V2 轻量纳入：B 端可复用数字资产——模板 / Prompt 包等，"开张率"抓手，**不做 C 端课程漏斗**）→ 按量 / 按次计费（V2：复用 TK-04 计量能力，作为订单**可选**计费单位，不主推——按量与业务价值脱节，适合测试期）→ 驻场 / 人天 / 人月（V2 末 / V3 再议，作为订单类型模板扩展）。**明确不纳入**：佣金抽成 / 带货分销、C 端知识付费漏斗、广告模式。
> 依据：《OPC经营收费模式调研》§1 全景图、§3.3、§3.7、洞察 4；WAIC 一次性购买 25 次提及 / 17%（溯源同论据核实记录 #10）。

#### 4.3.1 交付违约处置机制（v0.4 新增，FU-12–14）

> **设计目标**：回答发包方最担心的两个问题——"无法按时交付怎么办"与"中途放弃怎么办"。原则：**违约金只对未释放托管余额执行，平台全程只发指令、不碰钱；善后的首要目标是让发包方最快止损，其次是让违约变得昂贵。**

**① 违约情形与客观触发器**——全部基于平台原生信号（里程碑截止时间、交付物上传记录、站内通知响应），不读取项目管理数据，守住 J4 边界（不做项目管理 SaaS）：

| 违约类型 | 客观触发器 | 定性 |
|---|---|---|
| 逾期交付 | 里程碑截止 + 宽限期（默认 3 天）届满仍无交付物提交 | 按日累进违约金，封顶 |
| 中途放弃 · 主动申报 | OPC 在平台明确申报"无法继续履行" | 违约金减半，信用措辞较轻 |
| 中途放弃 · 推定（失联） | 催告发出后 N 天（默认 7 天）无任何平台动作（无交付物、无留言、无响应） | 全额违约金，信用记录更重 |
| 质量违约 | 同一里程碑打回 ≥3 次且无改进响应 | 发包方可发起终止 |

> **激励核心**：主动申报与消极失联差别对待——让 OPC 有动力尽早披露，发包方越早知道越早止损。企业最痛的不是"有人违约"，是"项目烂在原地无人负责"。
> 双方协商一致解除走同一资金处置与移交流程（④ 中 T4–T5），不计违约。

**② 违约金定价**（默认值，全部后台可配 + 版本公告，挂 CR-02 配置管理）：

| 情形 | 默认费率 | 封顶 |
|---|---|---|
| 逾期交付 | 该里程碑金额的 0.1%/日 | 该里程碑金额的 10% |
| 中途放弃（失联） | 未释放托管金额的 20% | 未释放托管余额 |
| 中途放弃（主动申报） | 未释放托管金额的 10% | 同上 |

> **两条定价约束**：① 《民法典》第 585 条及司法解释口径——约定违约金过分高于实际损失（一般以超过 30% 为判断参考）可能被调减，默认值压在安全线内；② **执行边界 = 未释放托管余额**——已验收释放的款项经持牌机构划出后不可逆，超出托管余额的损失只能由发包方凭证据走 FU-06 争议裁决主张。宁可少赔，必须可执行。

**③ 执行程序**（防"机器误判直接扣钱"）：

1. **催告前置**：触发器命中 → 站内 + 短信 + Webhook 三通道催告，启动宽限期 / 失联计时（同时满足《民法典》第 563 条解除合同的催告程序要件）。
2. **先裁后扣（大额）/ 先扣后诉（小额）**：金额 ≥ 结算阈值（默认 1 万）的，生成《违约认定书》→ 运营双人复核（OC-01）→ 申诉期（默认 7 天）届满且无申诉或申诉不成立，才发出扣款指令；小额直接执行、事后 7 天内可申诉。申诉期资金在持牌机构监管户内暂存，不进平台账户。
3. **全量留痕**：扣款指令写入 Contract 上下文 `BreachRecord`（复用 FactRecord 规范）：触发器、证据指针、金额计算式、执行人——可审计、可申诉。

**④ 善后 SOP 时限**（自违约认定书生成起算；触发与催告阶段见 FU-12）：

| 阶段 | 时限 | 动作 |
|---|---|---|
| T2 认定 | 宽限期 / 失联期届满 | 违约认定书生成；大额人工复核，小额自动 |
| T3 证据封存 | 即时 | 冻结该订单全部过程数据（append-only）：合同、里程碑记录、交付物版本史、洽谈记录、Agent 三元组日志 |
| T4 资金处置 | T+3 | 已验收未释放款**正常释放给 OPC**（防发包方借终止条款无偿占有已完成工作）；违约金扣除；剩余默认全额退发包方，**发包方可选择支付一部分换取可用半成品**（发包方选择权，非平台裁量）；已开发票冲红 / 作废归档 |
| T5 成果移交 | T+7 | 结构化移交清单（按订单类型模板）；**移交完成前冻结该 OPC 新投标资格**（平台最硬的非资金杠杆）；发包方确认接收或声明放弃后解除冻结 |
| T6 信用与重启 | T+7 | 信用负面条目（分档）；该单计入违约但**不计入按时交付率分母**（避免统计失真）；发包方一键复制需求结构化字段（SO-12）重发新单，可定向邀标更高 T 级 OPC，新单页标注"该需求曾有一次中途终止"（让新投标方知情定价）；OPC 申诉窗口贯穿 T2–T6 |

**⑤ 设计取舍记录**：

| 取舍 | 理由 |
|---|---|
| V1 不上投标保证金（SO-09 维持 V2） | 违约金（托管扣款）+ 投标资格冻结已覆盖对抗需求；保证金对冷启动期供给端是纯摩擦 |
| 触发引擎按主体类型共用 | 甲方侧对称触发器（如验收超时视为通过、拖款逾期自动采集）复用同一引擎，防止本机制沦为单向约束 OPC 的工具（§3.6 柠檬市场警告）；甲方细则另行设计 |
| 违约金受益人是发包方，平台零截留 | 平台截留违约金既无合同依据，也涉嫌变相资金归集（§8 红线 8） |
| 主动申报 / 申诉不向 Agent 开放 | 终止申报与申诉是产生法律后果的意思表示，必须由 Principal 本人做出（§6.4 责任归属铁律） |

> **合规硬约束（详见《资金与结算模式分析报告》）**：FU-05 的任何实现都**不得**让交易资金落入平台自有账户。平台只发分账指令，不触碰本金。FU-13 违约金处置同样适用本条：扣款以持牌机构指令实现，申诉期资金留在监管户。

### 4.4 信用积分与 OPC 认证

| 编号 | 需求 | 优先级 |
|---|---|---|
| CR-01 | 积分账本：append-only，记录事件类型、值、权重、来源上下文、关联对象、时间戳 | P0 |
| CR-02 | 权重配置后台：各类别权重、衰减系数 λ、金额分档规则，支持版本管理与变更公告 | P0 |
| CR-03 | 信用分计算引擎：定期批量重算 + 事件触发增量更新，结果可解释（能回答"为什么我是这个分"） | P0 |
| CR-04 | 信用详情页：分数构成、近期贡献、待改进项、同类分位（**透明可审计是公信力的基础**） | P0 |
| CR-05 | 认证等级自动评定与升降级，等级变更通知与申诉入口 | P0 |
| CR-06 | 积分→权益映射表（§3.4）在权限与排序中的实际生效 | P0 |
| CR-07 | **对外查询 API**：按 `principal_id` 或公开标识查询认证等级、信用分、核心能力标签 | P0 |
| CR-08 | 反作弊引擎：关联检测、互评环检测、异常模式检测 | P1 |
| CR-09 | **信用数据模型按 W3C VC 可导出格式设计**（DID + claims + proof 三段式），为 V2 接入国信平台预留 | P1 |
| CR-10 | **Agent 侧**：`get_credit_profile`（外部 Agent 只读查询，需授权 + 限流） | P0 |

### 4.5 词元（Token）交易

商业模式：**供应商代销 + 分账**（供应商直接对用户开票，平台抽佣）。

| 编号 | 需求 | 优先级 |
|---|---|---|
| TK-01 | 供应商入驻与资质审核；作为二级商户接入持牌分账体系 | P0 |
| TK-02 | **统一 OpenAI 兼容网关**：供应商一次接入，用户一个 API Key 调用多模型 | P0 |
| TK-03 | 商品目录：模型/能力、单价（输入/输出分别计价）、计费单位（按 token / 按次 / 订阅套餐） | P0 |
| TK-04 | 计量与计费：实时用量采集、账单生成、余额预警、预算上限与自动降级 | P0 |
| TK-05 | 分账：按配置比例在供应商与平台间分账，走持牌机构指令 | P0 |
| TK-06 | 长尾供应商降级方案：月结算额低于阈值时的替代结算路径（**需法务确认**） | P1 |
| TK-07 | 消耗看板：按应用/时间/模型多维分析，可导出对账单 | P1 |
| TK-08 | **政策对接台账**：按大连"token 加油包"口径输出结构化消耗记录（OPC 主体、模型、调用量、金额、时间），作为 50% 补贴季度兑现的申报凭证 | P1 |
| TK-09 | 智能路由（成本优先/效果优先/均衡）与故障自动切换 | P2 |
| TK-10 | **Agent 侧**：用量查询 Action、预算设置 Action、按量计费事件 Webhook | P1 |

### 4.6 内容与社区

| 编号 | 需求 | 优先级 |
|---|---|---|
| CO-01 | 公开课：课程发布（录播/直播/线下）、报名、签到、完课记录 | P0 |
| CO-02 | 完课与考核结果自动记入信用账本（学习成长类） | P0 |
| CO-03 | 内容提供者管理：讲师入驻、课程审核、分成结算 | P1 |
| CO-04 | 社区新闻/公告：发布、分类、置顶 | P1 |
| CO-05 | 活动管理：发布、报名、签到，参与记入信用账本 | P1 |
| CO-06 | **Agent 侧**：`list_courses`、`enroll_course` | P1 |

### 4.7 运营后台、企业画像与数据分析（v0.3 新增）

> **定位**：运营后台（OpsConsole）是平台运营与园区侧的「风控抓手 + 数据出口」，**不是项目管理 SaaS**（J4 已定）。数据隐私边界按 **D17 决议**执行：平台运营 / 财务 / 合规全局可见；OPC 与甲方互不可见对方完整项目列表，仅可见合作项目；**OPC 可见自己的全部 + 匿名汇总的行业趋势**。
>
> **版本策略**：本节多数条目 V2 落地；V1 不做功能，但交易、结算、信用、Token 四类台账自 V1 起全量留痕（§4.2–4.5），为本节全部数据服务提供地基。

| 编号 | 需求 | 优先级 |
|---|---|---|
| OC-01 | 运营后台框架与分权：运营 / 财务 / 合规角色分权，敏感操作（冻结结算、解冻、强制托管）双人复核（落地依赖 J1 / J3 决议） | P1 |
| OC-02 | 异常预警：订单逾期、验收超时、沟通异常、资金异动（大额 / 可疑交易口径联动 J5），阈值后台可配 | P1 |
| OC-03 | 生态看板（园区视角）：入驻转化漏斗、撮合 GMV、订单成交率、分行业分布、信用等级分布、政策兑现进度——对标 §1.5 成功指标自动出数 | P1 |
| OC-04 | 撬动比统计：财政投入 / 撬动社会资本 / 撬动税收增量等指标的采集与报表导出（口径待财务与园区确认，见 X10） | P2 |
| OC-05 | **OPC 经营视图**：我的收入与账期、订单漏斗、Token 消耗与成本（整合 FU-09 资金台账 / TK-07 消耗看板） | P1 |
| OC-06 | **企业画像 · OPC**：能力档案 + 信用档案 + 履约记录聚合展示（能力标签、历史交付行业分布、认证等级曲线），作为 CR-07 对外查询的数据基础 | P1 |
| OC-07 | **企业画像 · 发包方**：SO-11 履约画像的完整档案版，叠加工商 / 司法外部数据（X5）、信用维度、在招订单 | P1 |
| OC-08 | **行业趋势 · 匿名汇总**（落 D17）：交付时效中位数、争议率分布、成交周期、分行业订单量与成交率，对 OPC 端匿名展示 | P2 |
| OC-09 | **行业趋势 · 园区报告**：面向园区 / 政府的季度生态数据报告导出（招商与政策决策参考；仅基于平台内数据，见 §1.4 非目标） | P2 |
| OC-10 | **Agent 侧**：自身经营数据查询 Action（只读，仅限自身数据，遵循 D17 边界） | P2 |

> **边界说明**：① 企业画像仅聚合平台内数据（主体资料、履约、信用）+ 已核验的工商 / 司法公开数据（X5），不做社交化标签与评分以外的推断；② 行业趋势仅用平台内数据匿名汇总，不做宏观预测；③ 画像与趋势对个人主体的展示遵循个人信息保护合规（§7）。

---

## 5. 结算架构（可插拔设计）

### 5.1 为什么必须可插拔

撮合资金路径尚未最终选定，且不同阶段、不同金额的最佳方案不同。因此结算层设计为**适配器模式**，业务代码只依赖抽象接口，不依赖具体实现。

### 5.2 抽象接口

```
SettlementAdapter
  createEscrow(orderId, amount, payerAccountId)      → escrowId
  releaseMilestone(escrowId, milestoneId, splits)    → 分账指令
  refund(escrowId, amount, reason)                   → 退款指令
  split(escrowId, [{accountId, amount, role}])       → 多方分润
  getStatus(escrowId)                                → 状态
```

### 5.3 四种实现（V1 至少实现 None 与 LicensedSplit）

| 适配器 | 资金链路 | 适用 | 合规 |
|---|---|---|---|
| `None` | 不碰钱，仅流程留痕 | V1 快速上线 / 小额 | 无风险 |
| `NativeSplit` | 支付机构原生分账（微信/支付宝） | 分账比例 ≤30% 的简单场景 | 高 |
| `LicensedSplit` | **第三方持牌机构直连分账（默认）** | 主力场景，支持 0–100% 分账、延迟分账、逆向退款 | 高 |
| `BankCustody` | 银行存管 / 虚拟户分账 | 大额、多级分润 | 最高 |

**甄别红线**：凡底层资金先流入服务商对公账户的方案，一律排除。

> **周期扣费扩展（v0.4，V2）**：订阅类订单（FU-16）需在适配器接口追加 `createSubscription` / `chargeCycle`（**委托代扣 + 分账**组合指令）原语；V1 不扩展接口，仅由合同模型预留字段（§3.1）。选型持牌分账服务商时，须将「支持代扣 + 分账组合」列入甄别标准（X2，《资金与结算模式分析报告》§6 已同步增补）；自动续费提示义务等合规口径待法务确认（X14）。

### 5.4 分档路由

```
订单金额 < threshold（默认 1 万，可配） → 自动结算通道
订单金额 ≥ threshold                   → 里程碑托管 + 人工抽检通道
```

两条通道共用同一套结算接口，仅在风控环节不同。

---

## 6. Agent 原生设计（横切需求）

> **本节的设计前提（v0.2 修订）**
>
> Agent 原生的目标是**放大 OPC 的产能**，而不是在平台上创造一类新的自主经济体。
>
> 对一人公司而言，最稀缺的是时间与注意力。找单、投标、报名、跟进里程碑、对账开票——这些重复操作吃掉了一个人绝大部分的产能。Agent 原生的价值就是：**把这部分操作自动化掉，让 OPC 把时间花在真正的生产上。**
>
> 因此本节所有设计遵循三条约束：
> 1. **Agent 只代执行，不代承诺** —— 任何产生法律后果的动作，决策方必须是人
> 2. **Agent 无独立身份权益** —— 不拥有能力档案、信用、承接资格
> 3. **自动化必须在人类预设的规则内** —— 类比"限价委托单"而非"全权委托理财"（详见 §6.4）

### 6.1 核心范式：一次定义，多处暴露

采用 Agent-Native 架构（Rung 3）：**所有业务动作以 `Action` 为唯一原语定义一次，自动生成 UI 调用、REST 端点、MCP 工具、A2A 接口、CLI 命令、Webhook 触发器。**

```typescript
defineAction({
  name: "submit_bid",
  description: "提交投标方案。仅在订单处于竞标中且未过截止时间时可调用；同一订单同一主体重复调用将幂等返回首次结果。",
  schema: z.object({
    orderId: z.string(),
    quoteAmount: z.number().positive(),
    durationDays: z.number().int().positive(),
    proposal: z.string().min(100),
    idempotencyKey: z.string(),
  }),
  scope: "order:bid:write",
  readOnly: false,
  audit: { level: "full", retentionDays: 1095 },
  run: async (input, caller) => { /* 唯一一份业务逻辑 */ }
})
```

**工具描述必须为 LLM 推理而写**——它决定 Agent 能否正确选工具，而不是给人类读的文档。

### 6.2 MCP Server 工具集（精简，非 REST 全量暴露）

| 类别 | 工具 | scope | 说明 |
|---|---|---|---|
| 只读 | `search_orders`、`get_order_detail`、`list_my_bids` | `order:read` | |
| 只读 | `get_credit_profile`、`query_tier` | `credit:read` | 外部查询需授权 + 限流；查的是 **OPC 的信用**，不是 Agent 的 |
| 只读 | `list_courses`、`get_token_balance`、`get_usage` | `content:read` / `token:read` | |
| 操作 | `submit_bid`（幂等）、`enroll_course`、`accept_task` | `order:bid:write` 等 | 代提交，投标内容由 OPC 提供或确认 |
| 操作 | `submit_deliverable` | `fulfillment:write` | **代上传**交付物文件，交付物由 OPC 产出；不构成"Agent 完成交付" |

**明确不提供的工具**：`sign_contract`、`request_settlement`、`accept_award` 等产生法律承诺的动作；**v0.4 增**：`declare_termination`（主动申报终止）、`file_appeal`（违约申诉）——同为必须由 Principal 本人在 UI 完成的动作（FU-15、§4.3.1）。

> **不是"V1 暂不开放"，而是不作为产品方向。** 签约与结算必须由 Principal 本人在 UI 上完成（含电子签），这是责任归属的底线，不因 Agent 成熟度或主体信用等级而放开。

### 6.3 CLI

形态：npm / pip 可分发的 CLI 包，支持 `opc login`、`opc orders search`、`opc bid submit`、`opc agent run "<自然语言指令>"`。面向喜欢命令行的 OPC 与自动化脚本场景。

### 6.4 Agent 授权分级

> **v0.2 关键修订**：v0.1 的 L3 定义为「可承诺（自主签约/交付/结算）」，该方向已废弃。
> 新的 L3 是**规则内自动化**——**由人类预设规则，Agent 在规则命中的条件下自动执行**。
>
> 类比：**L3 是"限价委托单"，不是"全权委托理财"**。
> 限价单的条件（价格、数量、有效期）由人明确设定，触发即执行，人可随时撤销；
> 全权委托则是把判断权交出去——后者是本平台明确不做的。

| 级别 | 名称 | 决策方 | 行为 | V1 |
|---|---|---|---|---|
| **L1** | 代查询 | 人类发起 | 只读查询：发包信息、课程、公开信用、自身订单 | ✓ |
| **L2** | 代执行 | 人类发起，Agent 执行 | 提交投标、报名课程、领取任务、上传交付物 | ✓ |
| **L3** | 规则内自动化 | **人类预设规则**，Agent 在规则内执行 | 按预设条件自动执行（如"符合条件自动投标"），超规则则挂起待人类确认 | V2 |

**L3 规则的结构化定义**（人类显式创建，界面可见、可编辑、可撤销）：

```json
{
  "trigger": {
    "event": "order.published",
    "filters": { "tags": ["数据标注"], "budgetMax": 5000, "durationDaysMax": 7 }
  },
  "action": "submit_bid",
  "actionParams": { "quoteFormula": "budget * 0.9", "proposalTemplateId": "tpl_xxx" },
  "constraints": {
    "maxConcurrentBids": 3,
    "dailyCap": 5,
    "monthlyAmountCap": 30000,
    "validUntil": "2026-12-31"
  },
  "onOutOfRule": "suspend_and_notify"
}
```

**L3 的四条硬约束**（缺一不可，否则就变成了"全权委托"）：

| # | 约束 | 说明 |
|---|---|---|
| 1 | **规则必须人类显式创建** | Agent 不得自行创建或修改规则 |
| 2 | **规则必须有边界** | 额度上限、次数上限、有效期、条件过滤器，缺一则不允许启用 |
| 3 | **超出规则必须挂起** | 不在规则内的情况一律暂停并通知人类，不得"近似执行" |
| 4 | **每次执行必须可通知、可撤销** | 自动化动作产生后即时通知人类，人类可在后果固化前撤销 |

| 需求 | 说明 |
|---|---|
| Agent 注册 | Principal 在后台创建 Agent，生成 `agent_id` 与凭证（API Key / OAuth Grant） |
| 作用域化权限 | 每个 Action 声明所需 scope；凭证只授予必要 scope，可随时撤销 |
| 规则引擎（L3） | 规则的创建、校验、触发、挂起、通知、撤销全链路 |
| 凭证生命周期 | 支持多凭证、按 scope 分离、过期时间、即时吊销 |

> **关于"mandate"**：业界（AP2 / ERC-8004 等）用 mandate 指代人类给 Agent 的授权凭证。本平台的对应物是**上述规则集**——是条件明确、范围封闭、可撤销的授权，而非开放式的预算授权。二者在法律含义上差别极大，不要混用。

### 6.5 审计与确定性

| 需求 | 说明 |
|---|---|
| 幂等性 | 所有写操作强制 `idempotencyKey`，重试安全 |
| 结构化错误 | 错误响应包含 `code`、`message`、`retryable`、`suggestedAction`，供 Agent 据此决定重试、调整参数或转交人类（**注意：是错误处理策略的选择，不是业务决策权**） |
| 全链路审计 | 每条 Action 调用记录：`principal_id`、`agent_id`、`channel`、入参、出参、耗时、结果 |
| 可观测 | Agent 调用成功率、工具选择分布、失败原因分布（用于持续优化工具描述） |

### 6.6 机器可读的服务发现

提供 `/.well-known/agent.json`（A2A Agent Card 风格）与 `/llms.txt`，让外部 Agent 能自动发现本平台能力与调用方式。

---

## 7. 非功能需求

| 类别 | 需求 |
|---|---|
| 性能 | 竞标列表查询 P95 < 500ms；信用分查询 P95 < 200ms（对外 API 是高频调用） |
| 可用性 | 核心交易链路 99.9%；Token 计量链路 99.95% |
| 安全 | 全链路 HTTPS；敏感字段加密存储；Agent 凭证可撤销；操作审计留存 ≥3 年 |
| 合规 | 涉税信息季度报送字段可导出（810 号令）；四流一致（合同/订单/资金/发票）；个人信息保护合规 |
| 可扩展 | 信用权重、结算适配器、结算阈值、权益映射表**全部配置化**，不硬编码 |
| 可观测 | 业务事件全量埋点；Agent 调用链路可追踪 |

---

## 8. 合规与风控要点

| # | 红线 | 对应设计 |
|---|---|---|
| 1 | **禁止二清**：资金不得落入平台自有账户后再结算 | §5 结算适配器，全部走持牌机构 |
| 2 | **禁止可提现余额账户** | 无钱包功能；Token 走代销，无用户储值账户 |
| 3 | 积分不得具备金融属性 | 纯荣誉型，不可消耗/转让/兑换/提现 |
| 4 | 四流一致 | 合同、订单、资金、发票全量留痕，可关联查询 |
| 5 | 涉税信息季度报送 | 订单数据模型按报送口径设计（待财务确认字段清单） |
| 6 | VC 不得自建 | V2 接入国信数字凭证公共服务平台等持牌基础设施 |
| 7 | Agent 越权 / 责任归属不清 | 作用域化凭证 + L3 规则四约束（§6.4）+ 产生法律承诺的 Action 不向 Agent 开放 + 全链路 `agent_id` 审计 |
| 8 | **违约金执行越界（变相资金归集 / 平台截留处罚收入）**（v0.4） | 违约金仅以未释放托管余额为执行边界、走持牌机构指令（FU-13 / §4.3.1）；违约金受益人为发包方，平台零截留；先裁后扣程序 + `BreachRecord` 全量留痕 + 申诉期 |

---

## 9. 分阶段路线图

| 阶段 | 范围 | 关键里程碑 |
|---|---|---|
| **V1（MVP）** | 入驻审批、公开竞标+定向邀标、里程碑交付、分档结算（None/LicensedSplit 双适配器）、**交付违约处置与善后（FU-12–15，v0.4 新增）**、积分与认证（含对外 API）、Token 代销交易、公开课、MCP + CLI（L1/L2） | 跑通"入驻→投标→交付→结算→信用"一条完整闭环（含违约处置）；首批园区/政府场景订单导入 |
| **V2** | 信用反作弊引擎、智能路由、保证金与信用额度、长尾供应商结算、**周期订阅计费（FU-16，字段预留 V1 已就位）**、A2A 支持、**L3 规则内自动化**（自动投标/自动领任务）、**年审考核与清退（ID-10–14）、运营后台与数据服务（生态看板 · 双侧企业画像 · 匿名行业趋势 · 撬动比统计，§4.7）、典型系统集成（K 组）** | 信用体系具备真实约束力；OPC 的重复性操作被大规模自动化；园区侧获得可度量的生态数据出口 |
| **V3** | **平台总包（服务转售）模式**试点、VC 接入国信平台、垫付/保理、跨区域复制 | 平台从撮合方升级为交付方，毛利与壁垒显著提升 |

> **V3 平台总包模式说明**：平台作为合同主体向企业签约、全额开票、承担交付责任，再分包给 OPC。此模式**根本不存在二清问题**，同时解决"企业不敢把活交给一个人"与"OPC 扛不住账期"两个死结，其需求组织前提（链主企业提供场景、园区统筹并向 OPC 派单）已由成都芯谷「芯创社」（场景清单 + 揭榜挂帅）及成都元启共生（「先接业务、后分收益」的合伙制生态服务）公开实践；**平台作为合同主体的总包形态为本项目的机制设计延伸**——实际签约与开票主体模式待向运营方核实（澄清清单 X11），核实支持前不得对外表述为「总包已被实践验证」。代价是平台变重。

---

## 10. 假设与待澄清项

本 PRD 中以下判断为**基于研究的合理假设**，需业务方确认后固化：

| # | 假设 | 若假设不成立的后果 |
|---|---|---|
| AS-1 | 服务地域为大连高新区（可向外扩展） | 影响政策对接与合规口径 |
| AS-2 | 入驻主体类型包含个人、个体工商户、一人公司、1-5 人小微团队 | 影响主体数据模型与认证门槛 |
| AS-3 | 审批为 2 级（社区运营 → 园区/专家） | 影响审批流引擎复杂度 |
| AS-4 | 结算阈值默认 1 万元 | 仅影响默认配置，可后台调整 |
| AS-5 | 争议由平台裁决，重大争议引入专家委员会 | 影响运营人力配置 |
| AS-6 | Token 供应商以统一 OpenAI 兼容网关接入为主 | 影响网关研发投入 |
| AS-7 | 部署于公有云（若涉政务数据则改政务云） | 影响架构与合规 |
| AS-8 | 违约处置默认参数（v0.4）：逾期 0.1%/日（封顶里程碑 10%）、失联违约金 20% / 主动申报 10%、宽限期 3 天、失联 7 天、申诉期 7 天、处置时限 T+3 / T+7 | 全部后台可配，调整仅影响默认配置；若法务评估违约金调减风险高，可下调默认费率并强化 FU-06 实际损失举证路径 |

**完整待澄清问题清单（A–K 共 11 组 + X 外部确认包，含优先级，动态更新）见配套文档 `00-需求澄清清单.md`。**

**最需要优先推进的 5 项外部确认：**

1. 平台法人主体与资质情况（是否有支付/金融关联方）
2. 法务：预充值余额在属地是否适用单用途预付卡备案
3. 法务/财务：第三方持牌分账服务商甄别与选型
4. 财务：810 号令季度涉税报送字段清单（反向约束订单数据模型）
5. 运营：大连"token 加油包"补贴申报所需台账字段

> 上列 5 项均为 V1 阻塞项。**V2 启动前**需另备的外部确认（如 X14 自动续费 / 周期扣费合规口径，随 FU-16）已登记于澄清清单，届时提前启动，不占用本列。

---

## 附：设计方法论说明

本 PRD 采用组合方法论，而非单一 Design Thinking，原因在于：**本项目的难点不是"用户要什么"（业务方已有完整模块清单），而是"规则怎么定才不崩"**。

| 方法 | 解决的问题 | 应用位置 |
|---|---|---|
| 双钻 Double Diamond | 流程骨架 | 当前处 Define 阶段，重点收敛定义 |
| JTBD + 双角色画像 | 需求定义 | §2，人类与 Agent 两类 Jobs |
| **机制设计** | 信用、撮合、激励的防崩溃设计 | §3.3–3.5，衰减函数、权重、反作弊 |
| DDD 限界上下文 | 系统解耦 | §3.1，避免信用内核与交易耦合 |
| Agent-Native 范式 | 技术架构 | §6，一次定义多处暴露 |
